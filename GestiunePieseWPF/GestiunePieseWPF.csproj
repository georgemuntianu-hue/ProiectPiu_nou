<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
  </PropertyGroup>

  <!-- Excludem fișierele din proiectul consolă vechi care nu sunt necesare în WPF -->
  <ItemGroup>
    <Compile Remove="Program.cs" />
    <Compile Remove="MagazinAuto.cs" />
    <Compile Remove="Piesa.Auto.cs" />
  </ItemGroup>

</Project>
